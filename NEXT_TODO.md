# Next Todo

Список следующих задач для разработки проекта.

## In Progress

## Backlog

- [ ] **Проверить все 500 ошибки и заменить на соответствующие HTTP коды**
  Проанализировать все `throw new RuntimeException` и 500-е ответы в проекте,
  заменить на специфические исключения с правильными HTTP статусами:
  - 400 Bad Request — невалидные входные данные
  - 401 Unauthorized — неавторизованный доступ
  - 403 Forbidden — нет прав доступа
  - 404 Not Found — ресурс не найден
  - 409 Conflict — конфликт (дубликат, уже существует)
  - 422 Unprocessable Entity — бизнес-логика не позволяет

- [ ] **Создать систему миграции для проекта**
  Настроить Flyway или Liquibase для управления миграциями базы данных:
  создание таблиц, индексов, изменение схемы с версионированием

- [ ] **Проверить структуру базы данных**
  Проверить корректность constraints, reference keys (foreign keys),
  cascade delete/update правил, индексы, типы данных и связи между таблицами

- [ ] **Переписать сложные Hibernate запросы на ручные SQL**
  Заменить автоматически генерируемые Hibernate SQL-запросы (кроме простых CRUD)
  на оптимизированные ручные SQL запросы с использованием @Query или JdbcTemplate

- [ ] **Установить правило: SQL-запросы вместо Hibernate/JPQL для сложных операций**
  Все запросы к БД (кроме базовых CRUD: findById, save, delete) должны быть написаны
  вручную на SQL через `@Query(nativeQuery = true)` или `JdbcTemplate`.
  Запрещено использовать Hibernate-генерацию для сложных join, агрегаций, фильтров.
  Примеры базовых разрешённых операций: найти карту по ID, найти пользователя по email.

- [ ] **Установить правило: Lombok + Constructor Injection вместо @Autowired**
  Заменить @Autowired на полях на конструкторный injection через Lombok
  (@RequiredArgsConstructor или @AllArgsConstructor). Убрать все @Autowired
  с полей классов, использовать финальные поля и конструкторы

- [ ] **Вынести getCurrentUserId в общий метод/сервис**
  Создать общий метод или сервис для получения ID текущего пользователя
  (CurrentUserUtil или CurrentUserService), заменить дублирующийся код
  во всех контроллерах и сервисах

- [ ] **Придумать и разработать функционал прохождения card_desc**
  Продумать UX и логику: как пользователь проходит набор карточек,
  какие режимы (flashcard, quiz, input), как сохраняется прогресс

- [ ] **Разработать Learning Mode для дек (card_desc)**
  Режим обучения — пользователь проходит карточки деки в учебном режиме:
  показываем слово → пользователь угадывает/вводит ответ → сохраняем результат
  Нужно продумать: алгоритм повторения (Spaced Repetition / SRS?), хранение прогресса,
  статистику по деке (сколько выучено, сколько осталось)
